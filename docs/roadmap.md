# Future Ideas

- **Neovim**: Add `nvim/init.lua` with LSP + Treesitter, matching the VS Code tooling.
- **Cursor/Zed configs**: mirror VS Code key settings for AI-first editors.
- **Containers**: Provide `devcontainer.json` / `docker-compose` templates for Terraform + Python stacks.
- **Task runners**: Add `justfile` or `Makefile` snippets for bootstrap tasks (lint, fmt, test).
- **Secrets template**: Document how to keep `.ssh` and `.gitconfig.*` files encrypted (age/sops).

Feel free to open issues in this repo to track whichever idea matters most.
