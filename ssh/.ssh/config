# Global SSH defaults (Include additional host snippets from config.d)
Include ~/.ssh/config.d/*.conf

Host *
    AddKeysToAgent yes
    AddressFamily inet
    Compression yes
    ForwardAgent no
    HashKnownHosts yes
    IdentitiesOnly yes
    IdentityFile ~/.ssh/id_ed25519
    LogLevel INFO
    PreferredAuthentications publickey
    ServerAliveInterval 30
    ServerAliveCountMax 4
